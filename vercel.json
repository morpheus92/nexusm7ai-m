{
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node@2.x",
      "config": {
        "runtime": "nodejs18.x"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ]
}